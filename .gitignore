# deps & build
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.vite/
.node

# BCD 后端的运行时产物（审计日志 / worktree / agent 工作区）
.newide/

# electron 打包产物
release/

# env & secrets —— 永远不要进仓
.env
.env.*
!.env.example
*.local

# 本地状态 / 会话转储 / 归档
*.session.json
my-session.json
*.zip
*.tar.gz
*.log

# editor & OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# 项目专属
.claude
design-comparison
/api/

# 规划类文档：本地参考，不进仓
polaris_plan.md
merge_plan.md
*_plan.md

# 打包后端产物（scripts/build-backend.mjs 生成）
backend/
